class A {

  var x = 100;
  var y = 0;
  function setX(x) {
    
    this.x = x;
  }

  function setY(x) {
    this.y = x;
  }

  function add(a) {
    return this.x + a.x;
  }

  static function main() {
    var a1 = new A();
    var a2 = new A();
    a2.setX(6);

    a1.setX(30);
    
    return a1.add(a2);
  }
}